import 'package:flutter/material.dart';

class ResultService extends StatelessWidget {
  const ResultService({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
